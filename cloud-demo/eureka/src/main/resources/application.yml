server:
  port: 10086
# 微服务名称
spring:
  application:
    name: eurekaserver
eureka:
  client:
    service-url: #eureka地址信息 eureka 相互作注册 集群通信使用。 eureka 之间做数据交流。
      defaultZone : http://127.0.0.1:10086/eureka